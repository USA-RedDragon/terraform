.terraform/

# Lambda deployment artifacts, generated by `archive_file` on every plan.
#
# The function is uploaded INLINE (`filename` + `source_code_hash` on
# aws_lambda_function) rather than staged through S3, so this zip is a pure
# build product -- deterministic from lambda/*.py, never an input, and never
# something to commit.
*.zip
